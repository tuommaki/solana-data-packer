[package]
name = "solana-data-packer"
version = "0.1.0"
edition = "2021"

[features]
no-entrypoint = []

[dependencies]
serde = { version = "1.0.127", features = ["derive"] }
solana-program = { version = "1.9.8" }

[lib]
crate-type = ["cdylib", "lib"]
